# Validation Rules

1. Scope first: list target tests, target package, or target build profile.
2. Detect package manager before choosing commands.
3. No full test suite, global typecheck, or release publish unless explicitly approved.
4. Expo or EAS release checks require confirmed target profile and project context.
5. Electron package checks require confirmed target OS and release channel.

